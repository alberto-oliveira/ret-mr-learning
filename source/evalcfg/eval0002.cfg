[DEFAULT]
evalname=eval0002
k=10
        # Lowest value of M for RPP evaluation. Remember M=2 means column 1 from labels
lowM=2
        # Highest value of M for RPP evaluation. Remember M=8 means column 7 from labels
highM=8

# By convention, the name of the section and the 'name' parameter should be the same
# The name of the section is the exact name of the folder corresponding to the method
# The 'label' parameter should be a simplified name to refer to the method
[0000.bsl.ran]
name=0000.bsl.ran
label=RAN
color=255,255,0
marker=x
markersize=15
markeredgewidth=3
linestyle=-
linewidth=4